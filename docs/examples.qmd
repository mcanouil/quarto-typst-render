---
title: "Examples"
subtitle: "Typst, compiled as these pages build."
description: "Worked examples of the typst-render extension: maths, figures, cross-references, brand colours, data from R and Python, files, and slides."
# A ```{typst} block looks like an executable cell to Quarto's engine
# detection, which then selects Jupyter and fails for want of a kernel. The
# extension compiles those blocks itself, so the page needs no engine.
engine: markdown
---

Every figure in this section is compiled by the Typst binary Quarto ships, as the page is rendered.
The site asks for SVG, so the figures stay sharp at any zoom:

```yaml
extensions:
  typst-render:
    format: svg
```

## The pages

| Page | What it covers |
| --- | --- |
| [Math](examples/math.qmd) | The `math: typst` takeover, Typst maths syntax, and equations that reach every format. |
| [Figures and tables](examples/figures-and-tables.qmd) | Size, output format, resolution, alignment, and showing the source. |
| [Cross-references](examples/cross-references.qmd) | `label`, `cap`, and `alt`, for figures, tables, and any other reference type. |
| [Brand and theming](examples/brand-and-theming.qmd) | Reading `_brand.yml` inside a block, and one figure for the light and the dark side of a page. |
| [Data from R and Python](examples/data-from-r-and-python.qmd) | `typst_define()`, and reading computed values inside a block. |
| [Output and files](examples/output-and-files.qmd) | Preambles, external `.typ` files, multi-page output, input variables, and the cache. |
| [Presentations](examples/presentations.qmd) | The Reveal.js options: placement, line stepping, and sizing to the slide. |

: What each page of this section shows. {.striped .hover tbl-colwidths="[30,70]"}

## Inline expressions

An inline expression scales to the text around it, so a red word `{typst} #text(red)[hello]` sits in a sentence, and so does a formula `{typst} $ x^2 + y^2 = z^2 $`.

Give it alt text for a reader who cannot see it:

```markdown
The area is `{typst} $pi r^2$`{alt="pi r squared"} for a circle of radius r.
```

Without an `alt` attribute the Typst source is used instead.

The compilation options can be set on one expression as attributes:

```markdown
A big red `{typst} $pi r^2$`{format="png" dpi="300" foreground="#ff0000"} in a sentence.
```

::: {.callout-note}
Inline Typst is not available in PowerPoint, since Pandoc cannot put an image inside a run of text there.
Blocks work normally.
:::

## Source

The repository ships a short, standalone starting point you can copy: [`example.qmd`](https://github.com/mcanouil/quarto-typst-render/blob/main/example.qmd).

Every release attaches `example-render.tar.gz`, that same file rendered to HTML, Typst, PDF through each of the three LaTeX engines, RevealJS, Beamer, Word, and PowerPoint.
It is on the [latest release](https://github.com/mcanouil/quarto-typst-render/releases/latest), and is the way to see one source come out as figures in nine outputs.
