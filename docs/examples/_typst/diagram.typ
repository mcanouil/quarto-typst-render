// Compiled through the `file` option, in place of a block body.
#stack(
  dir: ltr,
  spacing: 8pt,
  circle(radius: 0.6cm, fill: blue.lighten(60%)),
  circle(radius: 0.6cm, fill: green.lighten(60%)),
  circle(radius: 0.6cm, fill: red.lighten(60%)),
)
